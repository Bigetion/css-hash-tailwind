import { createSimpleGenerator } from "../utils/generatorFactory";

const generateJustifySelf = createSimpleGenerator({
  prefix: "justify-self",
  property: "justify-self",
  themeKey: "justifySelf"
});;

export default generateJustifySelf;
