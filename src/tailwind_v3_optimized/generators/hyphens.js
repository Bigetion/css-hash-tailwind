import { createSimpleGenerator } from "../utils/generatorFactory";

const generateHyphens = createSimpleGenerator({
  prefix: "hyphens",
  property: "hyphens",
  themeKey: "hyphens"
});;

export default generateHyphens;
