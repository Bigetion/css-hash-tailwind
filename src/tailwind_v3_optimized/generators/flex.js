import { createSimpleGenerator } from "../utils/generatorFactory";

const generateFlex = createSimpleGenerator({
  prefix: "flex",
  property: "flex",
  themeKey: "flex"
});;

export default generateFlex;
